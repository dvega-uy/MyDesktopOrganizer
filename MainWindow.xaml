<Window x:Class="MyDesktopOrganizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DesktopBox" Height="300" Width="250"
        MinWidth="200" MinHeight="150" MaxWidth="600" MaxHeight="800"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="False"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        AllowDrop="True"
        Drop="Window_Drop">
        
    <Window.Resources>
        <!-- Estilo Oscuro para ContextMenu -->
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="#1F1F1F"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="5">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo Oscuro para MenuItem -->
        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8,10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="25"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center"/>
                                <Path x:Name="CheckMark" Grid.Column="0" Data="M 7,10 L 10,13 L 16,5" Stroke="#007ACC" StrokeThickness="2" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type Separator}">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
        </Style>
    </Window.Resources>
    <Border x:Name="MainBorder" 
            CornerRadius="15" 
            Background="#F2101010" 
            BorderBrush="#1FFFFFFF" 
            BorderThickness="1">
            
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.4" Color="Black"/>
        </Border.Effect>
            
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Opciones Generales" Click="OpenSettings_Click"/>
                <Separator/>
                <MenuItem Header="Cambiar Nombre" Click="Rename_Click"/>
                <MenuItem Header="Ocultar/Mostrar Título" Click="ToggleTitle_Click"/>
                <MenuItem x:Name="LockMenuItem" Header="Fijar Posición" IsCheckable="True" Click="ToggleLock_Click"/>
                <MenuItem Header="Color de esta caja..." Click="ChangeColor_Click"/>
                <Separator/>
                <MenuItem Header="Eliminar esta Caja" Click="DeleteBox_Click"/>
            </ContextMenu>
        </Border.ContextMenu>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="Header_MouseDown" MouseLeftButtonUp="Header_MouseUp">
                <Grid>
                    <TextBox x:Name="BoxTitle" Text="Nueva Caja" 
                               Foreground="White" 
                               Background="Transparent"
                               BorderThickness="0"
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Center" 
                               FontWeight="SemiBold"
                               FontFamily="Segoe UI"
                               IsReadOnly="True"
                               IsHitTestVisible="False"
                               CaretBrush="White"
                               LostFocus="BoxTitle_LostFocus"
                               KeyDown="BoxTitle_KeyDown"/>
                </Grid>
            </Border>
            <Border Grid.Row="1" x:Name="ContentArea" Margin="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <WrapPanel x:Name="FilePanel" Orientation="Horizontal" ItemWidth="70" ItemHeight="90"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</Window>
